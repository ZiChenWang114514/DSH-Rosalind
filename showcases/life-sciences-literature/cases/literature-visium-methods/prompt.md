# Spatial transcriptomics methods

Use the recorded title query to verify BayesSpace, Squidpy, SpotClean, Sprod, and SpatialExperiment in PubMed, then check each PMID with the PMC Article Dataset API. Run `python scripts/summarize.py` to recreate the five-method workflow map. Keep source metadata, project-authored workflow roles, and interpretation distinct. Invoke `mcp__rosalind__rosalind_open` with this case's task context, cite `outputs/rosalind-open-observation.json`, and state that the task chooser did not run a Rosalind scientific job.
