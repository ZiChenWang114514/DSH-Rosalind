# Nanobody binding assay evidence

Use the recorded PubMed queries to verify one BLI kinetics protocol and three records matching nanobody, surface plasmon resonance, and ELISA terms. Check the selected PMIDs with the PMC Article Dataset API, then run `python scripts/summarize.py` to recreate the assay-evidence map. Keep query matches, project-authored assay roles, and interpretation distinct. Invoke `mcp__rosalind__rosalind_open` with this case's task context, cite `outputs/rosalind-open-observation.json`, and state that the task chooser did not run a Rosalind scientific job.
