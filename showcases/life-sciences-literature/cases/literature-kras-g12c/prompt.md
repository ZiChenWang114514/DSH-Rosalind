# KRAS G12C literature trail

Use the recorded PubMed queries to verify PMIDs `24256730`, `26739882`, `31820981`, and `39215000`, then check their PMC availability. Run `python scripts/summarize.py` to recreate the dated four-record timeline. Keep bibliographic observations, project-authored labels, and interpretation distinct. Invoke `mcp__rosalind__rosalind_open` with this case's task context, cite `outputs/rosalind-open-observation.json`, and state that the task chooser did not run a Rosalind scientific job.
