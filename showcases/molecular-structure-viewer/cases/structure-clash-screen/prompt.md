# Whole-structure nonbonded overlap diagnostic

Using deposited model 1 of RCSB 1CRN, screen all admitted heavy-atom pairs for van der Waals overlap. State the radii, bond-inference rules, graph exclusions, alternate-location policy, number of tested pairs, and reporting threshold. Treat any row as a coordinate diagnostic and a zero count as conditional on the stated method; do not convert the result into a general validation score.

Inspect `outputs/rosalind-open-observation.json` as a separate launcher-availability observation. It records only that Rosalind Workbench was ready; do not describe it as a Rosalind scientific calculation.
