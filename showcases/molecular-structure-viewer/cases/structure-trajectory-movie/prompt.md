# Compact AdK trajectory inspection and movie rehearsal

Open the retained frame-zero PDB once, load `adk4AKE.psf` with `adk-equilibrium-5frames.dcd` as a topology-plus-trajectory pair, and verify five frames with 3341 atoms. Inspect frames 0, 2, and 4 while paused; use backbone alignment and no periodic-image correction. Validate a short once-mode trajectory movie before encoding. Start rendering or export only when the mounted viewer advertises source-relative publication, retain every returned job or artifact receipt, and cancel only a real queued or running duplicate job. Compare viewer observations with the local MDAnalysis metrics while preserving the original source-frame mapping. Invoke `mcp__rosalind__rosalind_open` with this case's task context, cite the 658-byte `outputs/rosalind-open-observation.json`, and state that the task-chooser response does not show that a Rosalind scientific job ran.
