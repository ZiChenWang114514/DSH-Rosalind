# DSH bundle verification record

This record describes the local build, registration, packed-bundle, and fresh-profile checks performed on 2026-08-30 from the current worktree.

## Environment

- Cordis: `4.0.1`
- DSH Tools: `0.1.1-rc.2`
- DSH Skill: `0.1.1-rc.2`
- live public network requests: disabled
- DeepSeek API calls: none
- packed bundle: generated by `npm run pack:bundle` with an independent npm cache on `D:`
- profile package manager: an isolated temporary installation of `pnpm 10.15.1`; no global package-manager installation was changed

## Checks executed in this revision

1. `npm run build` completed successfully and regenerated the 23 showcase manifests plus the host and client bundles.
2. `node scripts/verify-dsh-registration.mjs` registered 140 tools and 55 Skills:
   - 121 fixed operations from the capability manifest;
   - 6 Skill adapter tools, including all four Slide compatibility tools;
   - 13 Rosalind orchestration tools.
3. All 140 registered tools resolved through ToolRuntime, rendered structured output, exposed call/result presenters, and accepted a null-argument dispatch without an unstructured exception. Disposal removed all 140 tools and all 55 Skills.
4. Each Slide compatibility tool was dispatched with a missing test session and returned a structured scientific diagnostic: `slide_control_viewer`, `slide_run_analysis_from_chat`, `slide_run_pathology`, and `slide_query_scientific_layer`.
5. The PMC `article-dataset` contract and the `gtex-eqtl`, `clinvar-variation`, `ukb-topmed-phewas`, and `gnomad-graphql` provider contracts were dispatched with representative identifiers and parameters. Every request reached the explicit offline authorization check and returned `NETWORK_NOT_AUTHORIZED`; no public request was sent.
6. The 2,517,411-byte npm archive was inspected by `node scripts/check-packed-bundle.mjs`. Its 345 files include 55 project-authored `SKILL.md` files, 23 showcase manifests, the reproducible NGS workflow packages, the capability manifest's 121 fixed operations, all 6 Skill adapter names, all 13 Rosalind tool names, the four Slide compatibility names, the PMC contract in the built host file, and the provider IDs in both host and client files. Development-only `src`, `tests`, `node_modules`, third-party source mirrors, and experimental adapters were absent.

An earlier `npm pack` attempt encountered an `EEXIST` error in the shared global npm cache and did not create an archive. The final `npm run pack:bundle` succeeded with a dedicated cache on `D:`; the shared cache was left unchanged.

## Fresh-profile verification

`node scripts/verify-release-archive.mjs zichenwang114514-dsh-rosalind-0.3.0.tgz` installed the explicit archive into a temporary DSH evidence profile, confirmed DSH `0.1.1-rc.2`, read back all 55 Skills, found all 13 Rosalind tools, and executed representative local calls for Rosalind, NGS, Sequence, Structure, Slide, Literature, and Databases. The temporary profile was removed afterward. The report is `release/v0.3.0-archive-verification.json`.

The local browser preview suite passed 45 tests across 1280x800, 1440x900, 2048x1320, and 720x900; seven configuration-specific tests were skipped. The Web-profile test was among the skipped tests because `DSH_PROFILE_URL` was not supplied, so this record does not claim a fresh DSH Web page run for the final archive.

No push, publication, live public request, or DeepSeek API call was performed.
